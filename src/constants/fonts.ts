/**
 * Provides our fonts
 */

export enum FAMILY {
  bold = 'google-bold',
  medium = 'google-medium',
  regular = 'google-regular',
}

export enum SIZE {
  h1 = 30,
  h2 = 25,
  h3 = 20,
  h4 = 18,
  h5 = 16,
  medium = 16,
  regular = 15,
}

export const Fonts = {
  FAMILY,
  SIZE,
};
